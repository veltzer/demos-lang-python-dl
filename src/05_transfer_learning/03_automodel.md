# Exercise 03: AutoModel Embeddings

Load `AutoTokenizer` and `AutoModel` for `bert-base-uncased`.

Tokenize a sentence, run the model in eval mode, and print the shape of
`outputs.last_hidden_state`. For BERT-base, the hidden dim is 768.
