# Exercise 05: BatchNorm2d

Apply `nn.BatchNorm2d(num_features=4)` to a `(8, 4, 16, 16)` random input.

Print the per-channel mean and std of the output (computed over batch + spatial
axes). They should be approximately 0 and 1 respectively.
