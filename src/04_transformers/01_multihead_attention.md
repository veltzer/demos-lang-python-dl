# Exercise 01: nn.MultiheadAttention

Use `nn.MultiheadAttention(embed_dim=16, num_heads=4, batch_first=True)`.

Pass identical Q, K, V of shape `(2, 6, 16)` (self-attention). Print the output
shape and the attention-weights shape.
