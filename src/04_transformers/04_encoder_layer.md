# Exercise 04: nn.TransformerEncoderLayer

Use `nn.TransformerEncoderLayer(d_model=16, nhead=4, dim_feedforward=64, batch_first=True)`.

Pass a `(3, 8, 16)` input (batch=3, seq=8, dim=16). Print the output shape.
