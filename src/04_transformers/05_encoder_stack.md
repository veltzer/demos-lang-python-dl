# Exercise 05: Stack of Encoder Layers

Build `nn.TransformerEncoder(encoder_layer, num_layers=3)` from the previous
exercise's layer. Pass the same input through it and confirm the output shape
is unchanged.
