# Exercise 08: Reductions

Build a `(3, 4)` random float tensor with seed 0.

Print:
- the sum of all elements
- the per-column sum (shape `(4,)`)
- the per-row mean (shape `(3,)`)
- the index of the maximum value across the flattened tensor
