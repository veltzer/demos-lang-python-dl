# Exercise 05: Loss Functions

Demonstrate three common losses:

- `MSELoss` between `pred = [1.0, 2.0, 3.0]` and `target = [1.5, 2.5, 3.5]`.
- `BCEWithLogitsLoss` between logits `[1.0, -1.0]` and binary target `[1.0, 0.0]`.
- `CrossEntropyLoss` between logits `[[2.0, 0.5, 1.0]]` and class index `[0]`.

Print each loss value.
