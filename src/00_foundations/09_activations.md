# Exercise 01: Activation Functions

Apply each of `ReLU`, `Sigmoid`, `Tanh`, and `Softmax(dim=-1)` to the same
input tensor `[-2.0, -1.0, 0.0, 1.0, 2.0]` and print each result.

Notice how each squashes (or doesn't squash) the input differently.
