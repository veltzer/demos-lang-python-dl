# Exercise 00: A Single Linear Layer

Create an `nn.Linear(3, 2)` layer. Pass a random `(4, 3)` input through it
(batch of 4 samples, 3 features each) and print the output shape — should be
`torch.Size([4, 2])`.

Also print `weight.shape` and `bias.shape` of the layer.
