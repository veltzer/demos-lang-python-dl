# Exercise 02: nn.Sequential

Build a small MLP using `nn.Sequential`:

```
Linear(10 -> 32) -> ReLU -> Linear(32 -> 16) -> ReLU -> Linear(16 -> 2)
```

Push a `(8, 10)` random input through it and print the output shape.
