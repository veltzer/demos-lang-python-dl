# Exercise 04: Dtype and Device

Create a `float32` tensor `[1.0, 2.0, 3.0]`. Convert it to `float64` and to `int32`.

Print each tensor and its dtype.

Also print which device the original tensor lives on (CPU vs CUDA).
