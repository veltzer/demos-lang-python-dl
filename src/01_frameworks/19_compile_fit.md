# Exercise 02: compile + fit

Generate random `(200, 10)` features and 3-class labels. Compile the previous
model with `optimizer='adam'`, `loss='sparse_categorical_crossentropy'`,
`metrics=['accuracy']`. Fit for 5 epochs with batch size 32 and `verbose=2`.
