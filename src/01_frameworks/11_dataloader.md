# Exercise 01: DataLoader

Wrap the dataset from the previous exercise in a `DataLoader(batch_size=16,
shuffle=True)`. Iterate once over it and print the shapes of the first 3 batches.

Notice the last batch may be smaller than 16.
