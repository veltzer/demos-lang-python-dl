# Exercise 00: Create a Tensor

Create a 1-D PyTorch tensor containing the integers `1, 2, 3, 4, 5`.

Print the tensor, its `dtype`, and its `shape`.

## Expected output

```
tensor([1, 2, 3, 4, 5])
torch.int64
torch.Size([5])
```
