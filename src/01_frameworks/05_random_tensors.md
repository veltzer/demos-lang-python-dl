# Exercise 05: Random Tensors

Set the PyTorch manual seed to 42.

Then create:
- a `(2, 3)` tensor of uniformly distributed floats in `[0, 1)`
- a `(2, 3)` tensor of standard-normal floats
- a `(5,)` tensor of random integers in `[0, 10)`

Print all three.
