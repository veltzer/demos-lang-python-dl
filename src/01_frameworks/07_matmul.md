# Exercise 07: Matrix Multiplication

Compute the matrix product of a `(2, 3)` matrix `A` and a `(3, 2)` matrix `B`
using:

1. `torch.matmul`
2. the `@` operator
3. an explicit triple loop, and verify the result matches the first two.
