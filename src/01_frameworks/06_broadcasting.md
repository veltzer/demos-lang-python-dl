# Exercise 06: Broadcasting

Given a `(3, 4)` matrix `M` and a `(4,)` vector `v`, add `v` to each row of `M`
using broadcasting (no explicit loop).

Use `torch.arange` to build `M` and `v` so the output is deterministic.
