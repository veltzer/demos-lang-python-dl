# Exercise 06: Weight Decay (L2 Regularization)

Train the same linear regression model twice:

- Without weight decay.
- With `weight_decay=0.5` (very strong, exaggerated for effect).

Print the learned weight in both cases. Weight decay should shrink it noticeably
toward zero.
