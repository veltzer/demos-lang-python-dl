# Exercise 05: Mini-Batch Training Loop

Generate the same regression data as the previous exercise but train it with
mini-batches of size 16 using a Python `for` loop that shuffles indices each
epoch. Run for 20 epochs and print the final `w`, `b`.
