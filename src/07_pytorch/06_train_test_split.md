# Exercise 06: Random Train/Test Split

`torch.utils.data.random_split` cleanly splits a dataset.

Take a `TensorDataset` of 1000 random samples and split it into `800/200`
train/test datasets. Print the lengths of each.
